---
name: ansible_clone
runner_type: local-shell-cmd
description: "Clone a repository"
enabled: true
entry_point: ''
parameters:
  cmd:
    default: "git clone -b master git@bitbucket.org:exbet/ansible.git /opt/exbetio-ansible"
    immutable: true
