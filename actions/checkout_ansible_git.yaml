---
name: checkout_ansible_git
description: Initialize ansible repo for exbet.io deployment
runner_type: local-shell-cmd
entry_point: ""
enabled: true
parameters:
  sudo:
    immutable: true
    default: true
  kwarg_op:
    immutable: true
  timeout:
    default: 6000
  cwd: /opt/
  cmd:
    description: "Command to run"
    immutable: true
    default: git checkout git@bitbucket.com/exbet/ansible exbet-ansible
