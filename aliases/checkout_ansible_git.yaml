---
name: chatops.checkout_ansible_git
action_ref: exbetio.checkout_ansible_git
description: Checkout exbet.io ansible repo
formats:
  - display: "checkout devops ansible"
    representation:
      - "checkout devops ansible"
result:
  format: |
    {% if execution.status == 'succeeded' %}Output: ```{{ execution.result.stdout }}```{~}{% else %}
    *Exit Code:* `{{ execution.result.return_code }}`
    *Stderr:* ```{{ execution.result.stderr }}```
    *Stdout:* ```{{ execution.result.stdout }}```
    {% endif %}
  extra:
    slack:
      color: "{% if execution.result.succeeded %}good{% else %}danger{% endif %}"
      footer: "{{ execution.id }}"
      footer_icon: "https://stackstorm.com/wp/wp-content/uploads/2015/01/favicon.png"
